// TODO: Pagination and Search Bar will have strong connection.
// the state of pagination and input of search bar will be sent to backend
const usePagination = () => {
  return void 0
}

export default usePagination
